if has("autocmd")
  autocmd BufWritePost .vimrc.after source ~/.vimrc.before
end
nmap <Leader>vb :edit ~/.vimrc.before<CR>

call janus#disable_plugin('pep8', '.py files are broken, maybe because I didnt compile vim with python?')
